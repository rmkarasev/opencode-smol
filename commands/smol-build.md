---
description: Execute the latest .smol/plans/*.md via TDD
agent: coder
---

Use the `smol-build` skill. Read the latest plan from `.smol/plans/` and
execute its tasks one by one with strict RED → GREEN TDD.

Optional task hint: $ARGUMENTS
